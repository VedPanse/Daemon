# LineTrace Sensor Node

This example models manufacturer firmware for a telemetry-heavy line-following sensor module.

Exported control:
- `CALIBRATE(level)` for calibration mode `[0..3]`

Telemetry ideas (documented in source comments): `line_left`, `line_right`, `reflectance`.
